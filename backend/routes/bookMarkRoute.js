const router = require("express").Router();
const bookmarkController = require("../controllers/bookmarkController");
//const authController = require("../controllers/authController");

//router.post("/addBookMark", bookmarkController.addBookMark);
//router.post("/updatebookMark/:id", bookmarkController.updateBookMark);
//router.delete("/deletebookMark/:id", bookmarkController.deleteBookMark);

//router.get("/getBookmark/:id", bookmarkController.getBookMarksbyUserID);


module.exports = router;
